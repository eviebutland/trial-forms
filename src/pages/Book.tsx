import BookRoomForm from "../components/organisms/forms/BookRoom";

export const Book = () => {
  // Page to handle API calls
  // Add translations to the strings
  return (
    <>
      <h1>Book a room with us!</h1>
      <BookRoomForm></BookRoomForm>
    </>
  );
};

export default Book;
